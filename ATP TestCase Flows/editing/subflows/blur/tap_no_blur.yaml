appId: com.kodak.steptouch
---
# NO BLUR control (grey button, left of the radial circle). Removes the blur.
# Drawn on a custom surface with no accessible id/text, so tap by coordinate.
- tapOn:
    point: "40%,87%"
    label: NO BLUR control
- waitForAnimationToEnd:
    timeout: 2000
