appId: com.kodak.steptouch
---
# Orange/yellow checkmark — confirm sticker placement within category carousel.
# Tap ONLY the checkmark id (never a point fallback — a second tap hits Done/exit).
- tapOn:
    id: "saveRecentEditImageView"
- waitForAnimationToEnd:
    timeout: 1500
