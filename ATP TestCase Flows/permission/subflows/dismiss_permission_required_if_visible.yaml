appId: com.kodak.steptouch
---
# In-app modal when required runtime permissions were denied.
- runFlow:
    when:
      visible: ".*(?i)permission required.*"
    commands:
      - assertVisible: ".*(?i)some permissions are needed.*"
      - tapOn: "OK"
      - waitForAnimationToEnd:
          timeout: 3000
