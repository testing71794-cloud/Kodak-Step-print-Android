# TC_ID: COL_01
# Sheet: create collage (empty space)
# Steps: select → 1 photo → collage → layout → next → switch layout → next → toast
appId: com.kodak.steptouch
name: COL_01 - Create collage empty space toast
---
- launchApp:
    clearState: false
- runFlow:
    when:
      visible: "Create an account"
    commands:
      - runFlow: subflows/reach_gallery_for_collage.yaml
- runFlow: subflows/reach_gallery_fast.yaml
- runFlow: subflows/create_collage_one_photo_empty_space.yaml
