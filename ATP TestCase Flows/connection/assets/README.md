# Gallery printer icon reference (CO_04)

| File | UI state |
|------|----------|
| `gallery_printer_icon_connected.png` | Colored Kodak printer icon (auto-connected) |
| `gallery_printer_icon_disconnected.png` | Grey/white icon with red Bluetooth badge |

Maestro flows use `imageViewConnection` + `linearLayoutConnection` (connected) vs absent `linearLayoutConnection` (disconnected).
